/**
 * LeadCapture — REMOVIDO a pedido do stakeholder.
 * Arquivo mantido para não quebrar imports legados, mas componente não é mais
 * renderizado em page.tsx/Hero e não possui data-analytics-event="sample_cta_click".
 * Pode ser deletado em limpeza futura. Retorna null.
 */

export function LeadCapture() {
  return null;
}
